package com.dota2.heroes.enums

import com.dota2.heroes.R

enum class HeroAttribute(val id: Int, val attrColor: Int) {
    AGILITY(1, R.color.agility), STRENGTH(2, R.color.strength), INTELLIGENCE(3, R.color.intelligence)
}